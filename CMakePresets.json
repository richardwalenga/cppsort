{
    "version": 8,
    "configurePresets": [
        {
            "name": "default",
            "displayName": "default",
            "description": "This is a standard debug preset",
            "binaryDir": "${sourceDir}/build/${presetName}",
            "cacheVariables": {
                "CMAKE_BUILD_TYPE": "Debug"
            }
        }
    ]
}